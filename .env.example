# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Use a different base URL (e.g., for proxy or custom endpoint)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Use a different model (default is gpt-5)
# OPENAI_MODEL=gpt-5

# Optional: Adjust strictness level (normal, strict, lenient)
# ANKI_HERO_STRICTNESS=normal
